#pragma kernel CSMain

// texture material is set to 100% opaque.. so alpha channel is ignored!

RWTexture2D<float4> _RenderTexture00;

[numthreads(1,1,1)]
void CSMain (uint3 id : SV_DispatchThreadID)
{
    
    _RenderTexture00[id.xy] = float4(0, 1, 0, 1);

        
    
}
